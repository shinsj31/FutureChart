<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.soo.futurechart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".List_item" />
        <activity android:name=".Gragh1Activity" />
        <activity android:name=".Graph2Activity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".item_names" />
        <activity android:name=".Graph3Activity" />
        <activity android:name=".eventList_item"></activity>
    </application>

</manifest>